[project]
name = "monod-jax"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "jax",
    "optax",
    "numpy",
]

[tool.hatch.build.targets.wheel]
packages = ["monod_jax"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
